# Структура проекту Accessibility Evaluator

## Огляд
Проект складається з двох основних компонентів:
1. **accessibility_evaluator** - Python backend для аналізу доступності
2. **browser-extension** - Chrome/Firefox екстеншн для взаємодії з користувачем

---

## 📁 accessibility_evaluator (Python Backend)

### Нова структура:
```
accessibility_evaluator/
├── api/                    # API endpoints
│   └── app.py             # FastAPI сервер (189 рядків, було 2182!)
├── core/                   # Основна логіка аналізу
│   ├── evaluator.py       # Головний клас оцінки доступності
│   ├── metrics/           # Розрахунок метрик
│   │   ├── perceptibility.py
│   │   ├── operability.py
│   │   ├── understandability.py
│   │   └── localization.py
│   ├── analyzers/         # Специфічні аналізатори
│   └── utils/             # Допоміжні утиліти
│       ├── calculator.py  # Математичні розрахунки
│       ├── form_tester.py # Тестування форм
│       └── web_scraper.py # Скрапінг веб-сторінок
├── templates/              # Jinja2 HTML шаблони
│   └── index.html         # Головна сторінка веб-інтерфейсу
├── static/                 # Статичні файли
│   ├── css/
│   │   └── main.css       # Стилі веб-інтерфейсу
│   └── js/
│       └── main.js        # JavaScript веб-інтерфейсу
└── __init__.py
```

### Зміни:
- ✅ Перемістили `web_interface/backend/app.py` → `api/app.py`
- ✅ Видалили `web_interface/` директорію
- ✅ **Винесли 1969 рядків inline HTML в Jinja2 templates**
- ✅ Створили `templates/` з `index.html`
- ✅ Створили `static/` з CSS та JS файлами
- ✅ Спростили `app.py`: **2182 → 189 рядків (91% менше!)**
- ✅ Оновили `start_server.py` для нового шляху

### Запуск:
```bash
python3 start_server.py
# Сервер запуститься на http://localhost:8001
```

---

## 📁 browser-extension (Chrome/Firefox Extension)

### Нова структура:
```
browser-extension/
├── manifest.json           # Конфігурація екстеншина
├── src/                    # Вихідний код
│   ├── popup/             # UI popup екстеншина (ВСЯ логіка тут)
│   │   ├── popup.html
│   │   ├── popup.css
│   │   └── popup.js       # Використовує API :8001 + ініціалізація
│   ├── templates/         # HTML шаблони
│   │   ├── report-template.html  # Шаблон звіту
│   │   └── overlay-template.html # Оверлей аналізу
│   └── styles/            # Спільні стилі
│       └── analyzer.css
└── assets/                # Іконки, зображення
```

### Зміни:
- ✅ Реорганізували всю структуру в `src/`
- ✅ Винесли inline HTML в окремі template файли
- ✅ **Видалили рудиментарні файли (мертвий код):**
  - ❌ `form-tester.js` (~500 рядків дубліката Python коду)
  - ❌ `helpers.js` (~385 рядків дубліката Python утиліт)
  - ❌ `background.js` (~62 рядки - тепер ініціалізація в popup.js)
  - ❌ `content-scripts/analyzer.js` (~158 рядків мертвого коду - не використовувався)
- ✅ **Спрощено архітектуру до одного popup.js**
  - Вся логіка тепер в popup.js
  - Ініціалізація налаштувань перенесена з background.js
  - Видалено background service worker (не потрібен)
- ✅ Оновили `manifest.json` - прибрано background worker
- ✅ Унормували API URL - тепер тільки порт **8001**

### Встановлення:
1. Відкрийте Chrome → Extensions → Developer mode
2. Load unpacked → виберіть папку `browser-extension`
3. Переконайтеся що backend запущено на порту 8001

---

## 🔄 API Integration

### Попередня схема (проблемна):
```
popup.js → http://localhost:8001  ✅
analyzer.js → http://localhost:8000 ❌ (різні порти!)
background.js → Зайва ініціалізація
+ Дублювання коду в JS та Python
```

### Нова схема (виправлена):
```
popup.js → http://localhost:8001 ✅
  ↳ Уся логіка: UI + API + ініціалізація
+ Весь аналіз на backend, немає дублювання
+ Мінімальна архітектура без зайвих компонентів
```

### API Endpoints:
- `GET /api/health` - Перевірка стану сервера
- `POST /api/evaluate` - Аналіз сторінки за URL
  ```json
  {
    "url": "https://example.com"
  }
  ```
- `GET /docs` - Swagger документація

---

## 📊 Що було видалено/спрощено

### Видалені JS файли (рудиментарні):
1. **form-tester.js** (~500 рядків) ❌ ВИДАЛЕНО
   - Повний дублікат Python `form_tester.py`
   - Містив систематичне тестування форм
   - Причина: весь аналіз на Python backend

2. **helpers.js** (~385 рядків) ❌ ВИДАЛЕНО
   - Дублікат Python утиліт
   - Містив calculateContrast, parseColor, isElementVisible, etc.
   - Причина: весь аналіз на Python backend

3. **background.js** (~62 рядки) ❌ ВИДАЛЕНО
   - Тільки ініціалізація налаштувань при встановленні
   - Причина: логіку перенесено в popup.js, background worker не потрібен

4. **content-scripts/analyzer.js** (~158 рядків) ❌ ВИДАЛЕНО
   - МЕРТВИЙ КОД - ніде не використовувався
   - Не був включений в manifest.json як content_script
   - Причина: popup.js використовує власну функцію для підсвічування

#### app.py: 2182 → 189 рядків (-91%)
- ❌ Видалено 1969 рядків inline HTML/CSS
- ✅ Винесено в Jinja2 templates
- ✅ Винесено CSS в `static/css/main.css`
- ✅ Винесено JS в `static/js/main.js`

### HTML/CSS винесено в templates:
1. **app.py** → `templates/index.html` + `static/`
2. **popup.js** → `templates/report-template.html` (TODO)
3. **analyzer.js** → `templates/overlay-template.html`

---

## 🎯 Переваги нової структури

### 1. Чистота коду
- ✅ **Немає дублювання між JS та Python** - весь аналіз на backend
- ✅ **Немає inline HTML/CSS** - все в templates та static файлах
- ✅ **Один source of truth** - Python backend з axe-core

### 2. Легше підтримувати
- Логіка аналізу тільки в одному місці (Python + axe-core)
- HTML/CSS у Jinja2 templates та static файлах
- Чітка структура папок

### 3. Радикально менший розмір коду
- **app.py**: 2182 → 189 рядків (-91%)
- **Видалено мертвого коду**:
  - form-tester.js: ~500 рядків
  - helpers.js: ~385 рядків
  - background.js: ~62 рядки
  - analyzer.js: ~158 рядків
- **Всього видалено**: ~3900+ рядків дублікатів, мертвого коду та inline HTML
- Екстеншн: тільки один popup.js для UI, вся логіка на backend

### 4. Краща продуктивність та архітектура
- Аналіз на backend (Python + Playwright + axe-core)
- Екстеншн використовує менше пам'яті
- Веб-інтерфейс використовує Jinja2 templates (стандарт)

---

## 🚀 Як працювати з новою структурою

### Розробка Python backend:
```bash
cd accessibility_evaluator
# Змінити логіку аналізу в core/
# Додати нові метрики в core/metrics/
# Оновити API в api/app.py
python3 ../start_server.py
```

### Розробка екстеншина:
```bash
cd browser-extension
# Змінити UI в src/popup/
# Оновити templates в src/templates/
# Reload extension в Chrome
```

### Додати новий template:
1. Створити `.html` файл в `browser-extension/src/templates/`
2. Завантажити через `fetch(chrome.runtime.getURL('src/templates/...'))`

---

## 📝 Наступні кроки

### TODO:
- [ ] Перенести HTML генерацію звітів на backend (Jinja2 templates)
- [ ] Додати кешування результатів аналізу
- [ ] Створити unit tests для core modules
- [ ] Додати CI/CD для автоматичного тестування
- [ ] Опублікувати екстеншн в Chrome Web Store

---

## 📖 Документація

### API документація:
Після запуску сервера відкрийте: http://localhost:8001/docs

### Архітектура:
Детальний опис метрик та алгоритмів: `context/SESSION_PROGRESS_REPORT.md`

---

Створено: 2025-11-01
Версія: 1.0.1
